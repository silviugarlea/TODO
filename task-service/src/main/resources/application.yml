spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: tasks_database
  application:
    name: task-service
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka